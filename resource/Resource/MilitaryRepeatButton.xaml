<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="MilitaryTypography.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="MilitaryRepeatButton"
           TargetType="RepeatButton">
        <Style.Setters>
            <Setter Property="Padding"
                    Value="10, 5"/>
            <Setter Property="HorizontalContentAlignment"
                    Value="Center"/>
            <Setter Property="VerticalContentAlignment"
                    Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">

                        <Border x:Name="Panel"
                                CornerRadius="3"
                                BorderThickness="1"
                                BorderBrush="{StaticResource Brush_ButtonStrokeEnabled}"
                                Background="{StaticResource Brush_ButtonEnabled}">
                            <Border.Effect>
                                <DropShadowEffect Color="#FFFFFF"
                                                  Opacity="0.1"
                                                  BlurRadius="0"
                                                  ShadowDepth="2"
                                                  Direction="270"/>
                            </Border.Effect>
                            <TextBlock x:Name="ContentText"
                                       Style="{StaticResource TextBlock_Component_M}"
                                       Padding="{TemplateBinding Padding}"
                                       Text="{TemplateBinding Content}"
                                       HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                       VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsEnabled"
                                     Value="False">
                                <Setter TargetName="Panel"
                                        Property="BorderBrush"
                                        Value="{StaticResource Brush_ButtonStrokeDisabled}"/>
                                <Setter TargetName="Panel"
                                        Property="Background"
                                        Value="{StaticResource Brush_ButtonDisabled}"/>
                                <Setter TargetName="ContentText"
                                        Property="Foreground"
                                        Value="{StaticResource Brush_TextDark}"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="Panel"
                                        Property="BorderBrush"
                                        Value="{StaticResource Brush_ButtonStrokeHover}"/>
                                <Setter TargetName="Panel"
                                        Property="Background"
                                        Value="{StaticResource Brush_ButtonHover}"/>
                            </Trigger>

                            <Trigger Property="IsPressed"
                                     Value="True">
                                <Setter TargetName="Panel"
                                        Property="BorderBrush"
                                        Value="{StaticResource Brush_ButtonStrokePressed}"/>
                                <Setter TargetName="Panel"
                                        Property="Background"
                                        Value="{StaticResource Brush_ButtonPressed}"/>
                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>

    <Style x:Key="MilitaryRepeatButton_Image"
           TargetType="RepeatButton">
        <Style.Setters>
            <Setter Property="Padding"
                    Value="10, 5"/>
            <Setter Property="HorizontalContentAlignment"
                    Value="Center"/>
            <Setter Property="VerticalContentAlignment"
                    Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">

                        <Border x:Name="Panel"
                                CornerRadius="3"
                                BorderThickness="1"
                                BorderBrush="{StaticResource Brush_ButtonStrokeEnabled}"
                                Background="{StaticResource Brush_ButtonEnabled}">
                            <Border.Effect>
                                <DropShadowEffect Color="#FFFFFF"
                                                  Opacity="0.1"
                                                  BlurRadius="0"
                                                  ShadowDepth="2"
                                                  Direction="270"/>
                            </Border.Effect>
                            <ContentPresenter RecognizesAccessKey="True"/>
                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsEnabled"
                                     Value="False">
                                <Setter TargetName="Panel"
                                        Property="BorderBrush"
                                        Value="{StaticResource Brush_ButtonStrokeDisabled}"/>
                                <Setter TargetName="Panel"
                                        Property="Background"
                                        Value="{StaticResource Brush_ButtonDisabled}"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="Panel"
                                        Property="BorderBrush"
                                        Value="{StaticResource Brush_ButtonStrokeHover}"/>
                                <Setter TargetName="Panel"
                                        Property="Background"
                                        Value="{StaticResource Brush_ButtonHover}"/>
                            </Trigger>

                            <Trigger Property="IsPressed"
                                     Value="True">
                                <Setter TargetName="Panel"
                                        Property="BorderBrush"
                                        Value="{StaticResource Brush_ButtonStrokePressed}"/>
                                <Setter TargetName="Panel"
                                        Property="Background"
                                        Value="{StaticResource Brush_ButtonPressed}"/>
                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>
    
</ResourceDictionary>